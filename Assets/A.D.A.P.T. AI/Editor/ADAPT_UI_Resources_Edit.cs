using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEditor.UIElements;
using UnityEditor;

namespace ADAPT.UI
{
    public class ADAPT_UI_Resources_Edit : EditorWindow
    {
        /*public int value;
        Action action;
        Action.ResourceStruct str;
        // Start is called before the first frame update
        void Start()
        {

        }
        void OnGUI()
        {
            Event e = Event.current;
            //e.
            //if (e.commandName == "HOLA")
            Debug.Log("COMANDO " + e.commandName);
            Debug.Log(action.preconditions_list.Find(str => str.key.Equals(e.commandName)).selectedType);
        }
        // Update is called once per frame
        void Update()
        {
            Debug.Log("VALOOR: " + value);
        }*/
    }
}
